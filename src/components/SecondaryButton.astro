---
import Icon from './Icon.astro';
import { withBase } from '../utils/paths';
const { href = '/work', class: className = '' } = Astro.props;
---
<a class={`button button-secondary ${className}`} href={withBase(href)}><slot /><Icon name="arrow" size={17} /></a>
