---
import Icon from './Icon.astro';
import { withBase } from '../utils/paths';
const { href = '/contact', class: className = '' } = Astro.props;
---
<a class={`button button-primary ${className}`} href={withBase(href)}><slot /><Icon name="arrow" size={17} /></a>
